// use azothacore_common::{banner, log, utils::buffered_file_open, AzResult};
// use clap::Parser;
// use tracing::{info, warn};

fn main() {
    // let _wg = log::init_default("logs", "ConnectionPatcher");
    // banner::azotha_banner_show("Azothacore Connection Patcher", || {});
    // let vm = match ConsoleArgs::try_parse() {
    //     Err(e) => {
    //         return Err(e.into());
    //     },
    //     Ok(v) => v,
    // };

    // Ok(())
}

// #[derive(Parser, Debug)]
// #[command(author, version, about, long_about = None)]
// struct ConsoleArgs {
//     /// Path to WoW.exe
//     #[arg(short, long)]
//     path: String,
// }
